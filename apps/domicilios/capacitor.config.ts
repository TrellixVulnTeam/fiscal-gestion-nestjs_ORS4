import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'domicilios',
  webDir: '../../dist/apps/domicilios',
  bundledWebRuntime: false,
};

export default config;
