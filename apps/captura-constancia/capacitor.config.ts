import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'captura-constancia',
  webDir: '../../dist/apps/captura-constancia',
  bundledWebRuntime: false,
};

export default config;
